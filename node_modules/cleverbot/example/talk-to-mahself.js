
var CleverBot = require('../cleverbot');

var bot = new CleverBot();

function sayWhat(){
	bot.think(function(err, response){
		if(err){
			console.log(err);
		}else{
			console.log(response);
			//wait half a second so you don't spam their servers (and get blocked) 
			setTimout(sayWhat, 500);
		}
	});
}
sayWhat();
